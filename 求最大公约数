int main()
{
    int a,b,c,s;
    printf("请输入两个正整数\n");
    scanf("%d,%d",&a,&b);
    if(a>b)s=b;
    else s=a;
    for(s=s;s>=2;s--)
            if(a%s==0&&b%s==0)
        break;
    printf("结果是%d\n",s);
}
